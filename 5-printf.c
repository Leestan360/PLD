#include <stdio.h>

/**
 * main - main block
 * print a phrase followed with a new line
 * use printf
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
